#include <stdio.h>
#include <stdlib.h>
#include <string.h>

enum Verbose {
    VERBOSE_DISABLE,
    VERBOSE_ENABLE
};

enum Trim {
    TRIM_DISABLE,
    TRIM_ENABLE
};

void view_help();
